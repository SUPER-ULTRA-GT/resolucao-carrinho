import './style.css';
import produtos from '/src/data/produtos.json'
const Carrinho = () => {

    const add = (id) => {

    }
    return (
        {produtos.map(item => (
            <div>
                {item.}
            </div>
        ))}
        );
}

export default Carrinho;